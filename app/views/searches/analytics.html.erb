<h1>📊 Your Search Analytics</h1>

<h2>🔁 Recent Searches</h2>
<% if @searches.present? %>
  <ul>
    <% @searches.each do |query| %>
      <li><%= query %></li>
    <% end %>
  </ul>
<% else %>
  <p>No searches yet.</p>
<% end %>

<h2>🔥 Top Searches (By You)</h2>
<% if @top_searches.present? %>
  <ul>
    <% @top_searches.each do |query, count| %>
      <li><strong><%= query %></strong>: <%= count %> times</li>
    <% end %>
  </ul>
<% else %>
  <p>No search frequency data yet.</p>
<% end %>

<p><a href="/">⬅ Back to Search</a></p>
